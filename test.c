#include "minirt.h"
#include <stdarg.h>
#include <fcntl.h>

int main(int c, char **v)
{
	void *wind;
	void *init;

/* 	 init = mlx_init();
	wind = mlx_new_window(init,500,500, "test");
	mlx_pixel_put(init,wind, 10, 20,255);
	mlx_loop(init); */
	int	fd;
	fd =  open()
}